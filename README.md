# lsn

```sh
$ lsn -h
lsn v0.0.1
Print files, directories.
  -a	include hidden file, directory.
  -d int
    	recurse depth.
  -ex string
    	exclusion.
  -f	print full path.
  -fi string
    	filter.
  -od
    	only directory
  -of
    	only file.
  -v	print version.
```